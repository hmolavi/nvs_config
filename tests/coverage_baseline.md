# Coverage Baseline

Generated by the unit test suite (`tests/unit/`). Run `./run_unit_tests.sh` inside
`tests/unit/` to refresh. Hardware-only code paths (NVS flash I/O success
branches, timer callbacks, FreeRTOS task creation) are not exercised by the
unit suite and are excluded from these figures.

---

## Code Coverage (2026-03-20)

![coverage](/assets/2026_3_20_code_coverage.png)

| Source File          | Line % | Func % | Branch % |
| -------------------- | ------ | ------ | -------- |
| `src/nvs_config.c`   | 96%    | 93%    | 75%      |
| `src/secure_level.c` | 100%   | 100%   | 100%     |

_`nvs_config_console.c` is not compiled into the unit test binary (it depends on
`esp_console` which is not mocked). Console behaviour is tested indirectly via
the registry `set()` vtable in `test_console.cpp`._

_Remaining uncovered paths in `nvs_config.c`:_

- _`save_dirty_parameters_callback` (static timer callback is not directly callable from tests; exercised indirectly on real hardware)_
- _`#else` branch of `ESP_IDF_VERSION_MAJOR >= 5` preprocessor guards (FreeRTOS timer's path has only one branch compilation per binary)_

---

## Test History

| Date       | Unit Tests | Line Coverage | Branch Coverage | Notes                                                     |
| ---------- | ---------- | ------------- | --------------- | --------------------------------------------------------- |
| 2026-03-20 | 163        | 96%           | 75%             | Added init/save/migration/error-path tests; mock controls |
| 2026-03-19 | 149        | 74%           | 53%             | Initial unit/hardware split; first CppUTest host build    |
